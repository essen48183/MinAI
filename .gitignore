# Compiled binaries at the repo root only — prebuilt releases in bin/ are kept.
/minai
/minai.exe

# CMake build output
build/
build_test/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile.cmake

# md2pdf temp file
.md2pdf_tmp.html

# macOS
.DS_Store

# Object files
*.o
*.obj

# Visual Studio / MSVC local build leftovers.
# (Scoped so the prebuilt bin/windows-x86_64/minai.exe is preserved.)
/*.exe
build/*.exe
*.pdb
*.ilk
*.lib
*.dll
x64/
.vs/
